var express = require('express');
var router = express.Router();

/* GET users listing. */
/*routes.get('/users', function(req, res, next) {
    res.send(data.postListUsers);
});

module.exports = router;
*/